# Together See 1.0 验收范围

当前为 `0.2.0-beta.31` 公开测试版。开源不等于稳定版放行，自动化不能替代尚未完成的真机验收。

## 范围

单实例、匿名房间、同步观影、聊天弹幕、公开媒体解析、有限代理、JSON 持久化和 Docker 自部署。不包含账号、付费内容绕过、多实例和上传转码。

| 项目 | 要求 | 证据边界 |
| --- | --- | --- |
| 核心/权限 | 创建、门禁、接管、恢复、踢出、限流 | 已有服务/Socket 回归，真机需复测 |
| 同步 | 旧 source/revision 不覆盖、租约、缓冲边界 | 已有自动化 |
| 列表 | 元数据零换源、明确切源、自动连播 | 真实媒体仍需复测 |
| 本机偏好 | 关闭同步仍换源，本机操作不越权回写 | 自动化与历史可见交互验收 |
| Bilibili | 匿名公开、受限内容拒绝、直连优先、有界回退 | 夹具覆盖，上游可能变化 |
| 媒体安全 | 精确列表、跨房拒绝、SSRF、Range/HLS | 已有策略/协议回归 |
| 持久化 | 重启、损坏保护、容量、异步合并、最终关停 | 已有固定夹具回归 |
| 日志 | 无 token、原始 URL、房间内容或原始身份 | 字段/值白名单 |
| 浏览器 | 双端 video、重连、全屏、输入、弱网 | Chromium 模拟不是物理设备 |
| 供应链 | 锁文件、SBOM、审计、许可证 | 每次发布重新验证 |

## 正式版阻断

- 未解决 P0：安全边界失效、数据损坏、无法创建/加入/播放、无限重试。
- 未解决 P1：稳定错误换源、长期缓冲、权限混乱、消息失步、输入不可用。
- 最新固定候选未通过 `npm ci`、`npm run verify:1.0:full` 和官方源生产依赖审计。
- 未验证实际公网两层代理的 MP4 206、精确 Content-Range/字节数及 HLS 子资源。
- 未完成桌面和物理 Android 双端至少 30 分钟长播、弱网、后台及全屏输入。

短冒烟不能替代长播。记录失败原因、环境和证据，不修改阈值或重复尝试掩盖失败。Android 使用 [记录模板](ANDROID_1.0_TEST_RECORD.md)；原始敏感证据不得公开。
