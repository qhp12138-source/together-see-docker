# Together See Docker 部署

适用于单实例 Docker Compose，外层 Nginx 提供域名和 HTTPS。项目不依赖 PHP。以下使用示例域名和路径，不包含维护者生产信息。

## 首次部署

1. 获取公开仓库的固定提交，保留对应版本和校验信息。
2. 从根 `.env.example` 创建 `.env`，设置实际 `PUBLIC_ORIGIN`；生产不要用 `*`。
3. 默认 `WEB_BIND_ADDRESS=127.0.0.1`、`WEB_PORT=8080`，后端 3000 只在 Docker 网络中开放。
4. 根据真实反代层数配置 `TRUST_PROXY_HOPS`。外层 Nginx + 容器 Nginx 为 2；不要盲目信任外部转发头。
5. 保持 `ROOM_STORE_ENABLED=true`。根环境示例列出了房间、存储、超时、限流与媒体预算。

```bash
cp .env.example .env
# 先编辑 .env，再启动
docker compose up -d --build
docker compose ps
curl -fsS http://127.0.0.1:8080/api/health
```

本机体验可设置 `PUBLIC_ORIGIN=http://localhost:8080`。正式访问应统一通过同一 HTTPS 来源提供前端、API 和 Socket。

## 外层 Nginx / 宝塔

将仓库固定版本的 `nginx/baota-site.conf` include 到站点的 server 块中；不要复制旧版本片段后独立维护。该模板包含 Socket Upgrade、API、assets 和媒体代理规则。已存在同名 location 时先消除冲突，执行 `nginx -t` 成功后才 reload。

例如代码放在 `/opt/together-see`：

```nginx
include /opt/together-see/nginx/baota-site.conf;
```

模板默认反代 `127.0.0.1:8080`；若自行改端口，需一致调整并验证。证书、域名和站点日志由部署者管理。禁止在更高优先级的通用 API location 中绕过媒体隐私规则。

关键媒体规则如下，**仅用于理解和契约验证，不是完整站点配置**：

```nginx
location ~* ^/api/proxy(?:/|$) {
    access_log off;
    error_log /dev/null emerg;
    proxy_pass http://127.0.0.1:8080;
    proxy_http_version 1.1;
    proxy_set_header Range $http_range;
    proxy_set_header If-Range $http_if_range;
    proxy_cache off;
    proxy_buffering off;
    proxy_request_buffering off;
}
```

完整模板还设置可信转发头和流媒体超时。代理 token 位于查询参数，所有外层日志也必须避免记录这些参数，错误日志同样不能泄漏。媒体响应关闭缓存与缓冲，并透传 Range/If-Range。

## 媒体访问

`HLS_PROXY_ALLOWED_HOSTS` 默认包含 `*.bilivideo.com`。不应为了第三方站点改成任意主机代理。直接公网 MP4/HLS 需通过受限媒体探测，并精确加入当前房间列表后才能取得临时授权；验证缓存不是房间权限。网页提取仍有独立安全边界。

Bilibili 仅匿名公开内容，优先直连，失败最多有限兼容回退。不要填入登录 Cookie 或绕过会员、DRM、验证码。服务端代理消耗服务器出网带宽，不能保证第三方上游稳定。

## 更新与数据

- 使用明确的公开提交或公开发行标签，前后端从同一版本构建，不混用版本。
- 生产前自行制定备份与回退方案。私有部署中的快速流程不适合作为所有用户默认规则。
- 保留现有 `.env` 与 `server/data/rooms.json`；不要让源码同步、解压或清理覆盖运行数据。
- 关停允许持久化 writer 最终落盘；Compose 设置 70 秒宽限。savedAt 变化会改变文件摘要，应验证房间语义与健康加载，不用关停前后 SHA 相等判断数据完整性。
- 不发布 `.env`、房间快照、日志、录屏或备份；既有过期空房可按正常 TTL 被清理。

## 最小验收

1. `/api/health` 版本正确且 persistence healthy；首页、room、关键 JS 可访问。
2. 正式 Origin 允许，非法 Socket Origin 拒绝；polling 和 WebSocket Upgrade 正常。
3. 创建/加入、房主与普通成员权限、聊天、切源、自动同步和连播。
4. 从真实公网域名验证合法 MP4 精确 Range：206、Content-Range 与实际字节数一致；验证 HLS 子资源。
5. web/server 无异常重启/OOM，web 仅回环监听，Together See server 无宿主机端口映射。其他软件使用 3000 不等于本项目暴露。
6. 用实际设备验证自动播放、全屏、网络切换和长播。不能只用服务端日志代替画面。

1.0 放行要求见 [验收范围](1.0_ACCEPTANCE.md)。内置 Docker Web 仅复制静态资源，不对站点访问者提供开发 Markdown；它们在代码仓库中公开。
