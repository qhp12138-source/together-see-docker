# Together See 开发进度

此文档随公开代码持续维护。每次功能、配置或文档更新都追加日期、变化、验证结果和未覆盖项，不记录私人运维资料或真实房间内容。

## 当前阶段

代码基线为 `0.2.0-beta.31`，核心模块已实现，仍是公开测试版。是否达到 1.0 由验收矩阵决定，不用虚假的精确百分比代替证据。本仓库从独立快照开始，不引用私有历史标签。

| 模块 | 当前能力 | 后续重点 |
| --- | --- | --- |
| 房间 | 创建、密码、锁房、人数限制、空房过期 | 真机导航与恢复 |
| 身份 | 创建者恢复、房主与管理权接管、踢出 | 多端和后台切换 |
| 同步 | 版本化状态、控制租约、明确换源、弱网退避 | 真实弱网及长期漂移 |
| 列表 | 增删改、自动连播、元数据不重载当前源 | 浏览器自动播放限制 |
| 消息 | 聊天弹幕、昵称同步、成员系统消息 | 移动全屏输入 |
| 解析 | 公网 MP4/HLS、有限公开页面提取 | 第三方变更 |
| Bilibili | 匿名视频、分P、原弹幕、有界代理回退 | 风控和持续缓冲 |
| 代理 | 精确房间列表授权、Range、HLS、SSRF 防护 | 外层代理与资源预算 |
| 持久化 | JSON 异步合并写入、损坏保护、关停写入 | 容量和长期运行 |
| 交付 | Docker、锁文件、SBOM、协议与浏览器测试 | 开源协作、1.0 收口 |

## 开发演进

以下为历史开发的脱敏摘要。旧版本结果不等于当前版本已重新完成所有测试。

| 阶段 | 完成内容 |
| --- | --- |
| 2026-07 / 0.1 系列 | 房间、同步、聊天弹幕、列表、本地文件、密码锁房和部署基础 |
| beta.1–5 | Bilibili 匿名适配、品牌界面、资源和授权保护 |
| beta.6–8 | 创建凭据两阶段保存、身份有限恢复、播放权与管理权接管 |
| beta.9–10 | 元数据与媒体身份分离、控制租约、陈旧状态拒绝、弱网恢复 |
| beta.11–12 | 旧源 seek 不倒回；直链代理匹配房间列表。beta.11 安全验收未通过，不推荐使用 |
| beta.13–16 | 入房和持久化完整性、会话撤销、限流、连播持久化、关闭同步仍换源 |
| beta.18–25 | 异步快照、关停容量、确定性媒体夹具、双端浏览器与长播门禁 |
| beta.26 | 外层 Range 透传和代理查询参数日志保护 |
| beta.27–28 | 诊断原因过滤修复、回退保留播放意图、按钮居中 |
| beta.30–31 | 权威缓冲冻结/恢复、弹幕调度、BV 匿名 pagelist/playurl 回退及 412 冷却 |

## 2026-09-09：首次公开整理

- 原私有仓库、本地开发目录和完整历史保留，不修改生产。
- 公开副本保留运行源码、测试、锁文件、生成媒体夹具及 Nginx 模板。
- 整理 README、部署、排障、进度和验收文档；新增贡献、安全报告与第三方许可证说明。
- 不导入私人服务器地址、备份路径、会话记录、房间实例记录或私有 Git 历史。
- 本轮独立副本执行 npm ci（禁用包安装脚本）、完整 npm run verify:release 全通过，包含类型/前端/凭据/发布/SBOM/存储/HTTP/解析/Bilibili/代理/Socket；官方生产依赖审计为 0 vulnerabilities（2026-09-09）。
- 发布清单 98 文件；68 个运行源码、配置和测试相关文件与原基线逐项一致（文本统一换行后比较）。公开文档链接、禁入路径和已知私人上下文检查均通过；Dockerfile 仅新增许可证复制。
- 未执行 Docker 构建/部署、浏览器长播、Android/iOS 真机或外部媒体实播。本次是开源整理，不替代 1.0 验收。

## 下一阶段

1. 真实第三方视频连续长播，验证回退有界、暂停意图正确。
2. 双端切源、开关原弹幕、新增非当前项，验证不倒回、不重载。
3. Android Chrome/WebView 和 Safari 实测网络切换、后台、全屏输入。
4. 清零阻断缺陷，完成固定候选全量验收后再发布 1.0。

账号、SQLite/Redis、多实例、上传转码与 P2P 分发不进入当前 1.0 范围。
