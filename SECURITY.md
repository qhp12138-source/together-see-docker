# 安全政策

当前仅维护最新公开 beta，不承诺旧测试版本的安全更新或生产 SLA。

## 私密报告

优先使用 GitHub 仓库 Security 页面中的 Report a vulnerability（需维护者启用）。若入口不可用，仅创建“不含利用细节”的联系请求，由维护者提供私密渠道；不要在公开 Issue、PR 或日志中发送漏洞载荷、凭据或私人信息。

报告可包含受影响版本、影响范围、最小复现与修复建议。仅在你控制或获得授权的实例上验证，不探测第三方服务器。

## 部署边界

- HTTPS 和准确的 PUBLIC_ORIGIN/TRUST_PROXY_HOPS。
- 媒体仅允许受控公网目标；保留 DNS/重定向/SSRF 与房间列表授权。
- 管理 token、恢复码、签名媒体 URL、代理查询与原始日志均应视为敏感信息。
- 保护 .env、server/data、备份和测试证据；不要暴露源码目录作为静态站点根。
- 定期更新依赖并运行官方审计；免责声明不能代替安全运维。
