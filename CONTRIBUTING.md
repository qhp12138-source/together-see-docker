# 贡献指南

欢迎通过 Issue 讨论问题或提交小范围 PR。先说明目标、复现步骤和兼容风险，不提交第三方受限内容或未授权扫描结果。

## 开发约定

- 运行与测试命令在 server/，锁文件必须保留；不要并发构建 dist。
- 复用现有 JavaScript/TypeScript、Socket 事件和状态机，不无关重构。
- 修改同步/权限/解析时增加针对性负面回归；浏览器问题补真实媒体元素用例。
- 每次代码、配置或文档更新同步 PROJECT_PROGRESS.md，写明日期、验证和未覆盖项。
- 提交前至少运行 npm run verify:release；播放器改动运行浏览器与相关弱网测试。
- 不提交 .env、运行数据、日志、截图中的私人信息、AI 工作目录、node_modules 或 dist。
- 发布标签保持不可变。公开发行的提交身份与私有历史不同，不导入私有祖先或标签。

## 报告问题

提供公开版本/提交、系统/浏览器版本、脱敏操作步骤、预期/实际结果和复现频率。视频问题说明直链/HLS/Bilibili、是否自动同步、控制策略；不要附带签名 URL、Cookie、token、恢复码或房间快照。

安全漏洞请走 SECURITY.md，不公开利用细节。
